export function processMessageContent(content: string): string {
  // Por enquanto, apenas retorna o conteúdo como está
  // Aqui você pode adicionar processamentos futuros como formatação de links, emojis, etc.
  return content
}
